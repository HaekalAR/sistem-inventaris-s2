export const metadata = {
    title: "Register",
    description: "Create a new account to start managing your inventory and products efficiently.",
};

export default function RegisterLayout({ children }) {
    return <>{children}</>;
}
