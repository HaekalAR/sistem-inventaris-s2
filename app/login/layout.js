export const metadata = {
    title: "Login",
    description: "Log in to your account to manage your inventory and track stock levels.",
};

export default function LoginLayout({ children }) {
    return <>{children}</>;
}
